enum Pages { games, more }

enum SquareColor { white, orange, green }

enum GameCategory { memory, speed, maths }

enum AuthType { google, anonymous }

enum SwipeDirection { left, up, right, down }

enum AirplanesType {
  vertical,
  horizontal,
  quadrangle,
  pentagon,
  hexagon,
}

enum QuestionType {
  lowestNegative,
  lowestPositive,
  highestNegative,
  highestPositive,
}

enum BombStatus { active, exploded, defused }
