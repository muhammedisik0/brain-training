import 'dart:ui';

import 'package:flutter/material.dart';

class ColorConstants {
  ColorConstants._();

  static const Color purple = Color.fromRGBO(74, 8, 255, 1);
  static const Color orange = Color.fromRGBO(255, 165, 0, 1);
  static const Color deepPurple = Color.fromRGBO(52, 6, 179, 1);
  static const Color white = Color.fromRGBO(255, 255, 255, 1);
  static const Color black = Color.fromRGBO(0, 0, 0, 1);
  static const Color grey = Color.fromRGBO(85, 85, 85, 1);

  static const Color memory = Color(0xff5837d0);
  static const Color speed = Color(0xff0C134F);
  static const Color maths = Color(0xff3C486B);
  static const Color attention = Color(0xff1363DF);

  static const Color whiteWithOpacity = Color.fromRGBO(255, 255, 255, 0.35);
}
